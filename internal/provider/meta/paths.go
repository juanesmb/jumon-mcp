package meta

// platformName is the gateway provider id and explore_platform platform key.
const platformName = "meta"

// DefaultGraphAPIVersion is documented for agents; authoritative version is in mcp-ads-manager META_GRAPH_API_VERSION.
const DefaultGraphAPIVersion = "v25.0"

// AdAccountIDPrefix is required on Marketing API ad account ids (e.g. act_123456789).
const AdAccountIDPrefix = "act_"
